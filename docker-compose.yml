version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/src:/app/src
      - cargo-cache:/usr/local/cargo/registry
    environment:
      - RUST_LOG=debug

volumes:
  cargo-cache:
